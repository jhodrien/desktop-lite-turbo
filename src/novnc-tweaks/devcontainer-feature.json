{
    "name": "noVNC tweaks",
    "id": "novnc-tweaks",
    "version": "1.0.0",
    "description": "A feature to adjust noVNC",
    "options": {
        "prompt": {
            "type": "boolean",
            "default": false,
            "description": "Should noVNC display a connect prompt"
        },
        "resize": {
            "type": "string",
            "enum": [
                "none",
                "remote",
                "scale"
            ],
            "default": "remote",
            "description": "Choose how resizing should be handled"
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/desktop-lite"
    ]
}
